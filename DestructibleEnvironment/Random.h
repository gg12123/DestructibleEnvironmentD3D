#pragma once
#include <assert.h>

class Random
{
public:
	static float Range(float min, float max)
	{
		assert(false);
		return min;
	}

	static int Range(int minInclusive, int maxExclusive)
	{
		assert(false);
		return minInclusive;
	}
};